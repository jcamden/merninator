export { ensureType } from './utils/ensureType';
export { registerUser, loginUser, setAuthToken, loadUser, loadUserAuthDispatch } from './utils/authUtils';

// self-invoking async function
// (async (): Promise<void> => {})();
