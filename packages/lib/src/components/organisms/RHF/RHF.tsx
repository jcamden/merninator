import React from 'react';

interface RHFProps {
  handleSubmit: () => void;
  register: () => void;
}

export const RHF: React.FC<RHFProps> = ({ handleSubmit, register }) => {
  return <form></form>;
};
